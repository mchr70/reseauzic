{% extends 'base.html.twig' %}

{% block title %}Hello MemberController!{% endblock %}

{% block body %}
    {% for thread in app.user.threads %}
        <p><a href="{{ path('thread', {id: thread.id}) }}">{{ thread.title }}</a></p>
    {% endfor %}
{% endblock %}