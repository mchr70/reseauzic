{% extends 'base.html.twig' %}

{% block title %}Hello MemberController!{% endblock %}

{% block body %}
    <h3>Votre profil</h3>
    <p>Mail: {{ app.user.email}}</p>
    <p>Âge: {{ app.user.birthDate | age }} ans</p>
    <p>Date d'inscription: {{ app.user.registrationDate|date("d/m/Y")}}</p>
    {% if app.user.gender == 1 %}
        <p>Sexe : homme</p>
    {% else %}
        <p>Sexe : femme</p>
    {% endif %}
{#}
    <p>Code postal: {{ app.user.zipCode }}</p>
    <p>Ville: {{ app.user.city }}</p>
    <p>Adresse: {{ app.user.address }}</p>
    <p>Téléphone: {{ app.user.phone }}</p>
    <p>Description: {{ app.user.about }}</p>
    <p>Influences: {{ app.user.influences }}</p>
    <p>Matériel: {{ app.user.material }}</p>
    <p>Genres de musique:</p>
    {% for genre in app.user.genres %}
        <li>{{ genre.name }}</li>
    {% endfor %}
    <p>Instruments pratiqués: </p>
    {% for uil in app.user.userInstrumentLevels %}
         <li>{{ uil.instrument.name }}, niveau {{ uil.level.name }}</li>
    {% endfor %} #}

    {{ form(form)  }}
{% endblock %}
